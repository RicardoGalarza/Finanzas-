# FlujoClaro Frontend

React + TypeScript + Vite + PWA.

```bash
npm install
npm run dev
```

Variables opcionales:

- `VITE_API_URL` — vacío por defecto (usa proxy de Vite hacia `http://localhost:8080`)
